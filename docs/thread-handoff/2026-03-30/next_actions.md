# 下一步直接工作

## P0：先修复版本命中率回退
目标：
- 把 `versionFinalHit` 从 `18/28` 恢复到至少 `22/28`
- 把 `versionCandidateHit` 从 `18/28` 恢复到至少 `23/28`

这一步必须先做，做完再谈更大范围的新规则。

## P1：在正确 `.venv` 环境下，逐条审计剩余 `recall_miss`
优先样本：
- `recording-a小调钢琴协奏曲-肯普夫-and-多拉蒂1959-full`
- `recording-a小调钢琴协奏曲-肯普夫-and-多拉蒂1959-partial`
- `recording-a小调钢琴协奏曲-里赫特-and-费伦奇克1954-full`
- `recording-a小调钢琴协奏曲-劳拉-and-怀特1951-partial`
- 其余仍为 `recall_miss` 的 partial/full 长尾样本

每条样本至少做三件事：
1. 看目标链接标题与简介到底携带了哪些关键证据
2. 看当前真实执行的 query 是否覆盖了这些证据
3. 看这些 query 在 `Bilibili / YouTube / Apple` 上的实际 top results 是否已经把正确版本带回

## P2：优先强化 bundled upload（合集上传）场景
当前硬 miss 的共性是：
- 正确页面常不是“单作品干净标题”
- 而是“多作品/合集/现场混合上传”
- 关键证据大量出现在：
  - 简介
  - 日期
  - 地点
  - 乐团缩写
  - 节目单式标题

下一线程应优先把这些 bundled 线索从“只在 browser rescue 中有用”，推进到：
- 主 query 构造
- 主结果排序
- same-recording 评分

## P3：继续保留平台独立候选，不要重新退回全池混排
当前原则：
- 各主平台独立给候选配额
- `green`（绿区）和 `yellow`（黄区）分开
- 不要因为某个平台更强，就把另一个平台正确但弱证据的候选整体压没

但请注意：
- 这不是当前最高收益点
- 当前最高收益仍是召回

## 建议执行顺序
1. 先跑 `.venv` 下的 `pytest` 与一轮舒曼钢协完整评估
2. 对剩余 `recall_miss` 做样本级 query/result 审计
3. 只做最小必要代码修改
4. 再次完整评估，确认是否恢复到 `22/28` 和 `23/28`
5. 只有恢复后，才继续推进更广泛的平台与排序优化
