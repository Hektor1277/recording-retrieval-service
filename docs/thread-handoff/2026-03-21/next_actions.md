# 下一步动作

## 第一优先级
1. 继续优化 `spring-lead-only`
   - 这是当前最明确的残余缺陷。
   - 目标：当只给 `Jean Fournier` 时，也能更稳定回到 `Jean Fournier & Ginette Doyen` 的目标链接，而不是单人替代版本。

2. 稳定批量回归
   - 当前单条或聚焦回归往往优于全量 8 条批量回归。
   - 重点排查：
     - 连续请求后的站点波动
     - `streaming` 阶段在整批运行中的退化
     - 是否需要对批量回归中的每个场景做更强缓存或 host 级限流

## 第二优先级
3. 清理遗留乱码 warning 文案
   - 部分旧 warning 仍是乱码。
   - 功能不受影响，但会影响 UI 可读性和交接质量。

4. 强化 `Arrau full` 与室内乐批量场景
   - 当前聚焦场景命中优于全量批量场景。
   - 需要缩小“单条可命中、整批回归掉点”的差距。

## 第三优先级
5. 如果用户继续要求提高智能性
   - 可考虑引入“人物中文名/外文名对照表”的自动补写逻辑
   - 或继续增强 `LLM` 对作品别名、语言变体的辅助确认
